@model Arad.Models.Account

@{
    ViewBag.Title = "Register";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <h1 class="col-md-offset-2">Register</h1>
    <hr />
    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.Label("PhoneNumber", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.PhoneNumber, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.PhoneNumber, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Password", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Password, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Password, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">نقش</label>
            <div class="col-md-10">
                <select name="RoleId" class="form-control">
                    <option value="2">مالک سالن ورزشی</option>
                    <option value="3">مالک تیم</option>
                    <option value="4">مربی</option>
                    <option value="5">ورزشکار</option>
                </select>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @Html.ActionLink("Back", "Index", new object { }, new { @class = "btn btn-default btn-sm" }) |
                <input type="submit" value="Register" class="btn btn-success btn-sm" />
                <p style="margin-top:10px;">You can <a href="@Url.Action("Login", "Home")" class="btn btn-primary btn-xs">Login</a> if you have already registered.</p>
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
